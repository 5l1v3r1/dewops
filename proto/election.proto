syntax = "proto3";

import "google/protobuf/empty.proto";

package proto;

message Vote {
    string name = 1;
    string votefor = 2;
}

service Voting {
    rpc Elect (Vote) returns (google.protobuf.Empty);
}